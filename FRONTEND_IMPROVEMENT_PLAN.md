# Frontend Improvement Plan

本文档整理「墨 · 影 字幕工坊」前端后续需要新增和优化的功能点，目标是把当前界面从“功能可用”推进到“字幕生产工作台”。

## 目标

- 让用户更少跳转、更少猜测、更少重复操作。
- 围绕“写文本 → 分段预览 → 调整参数 → 导出字幕/语音”的真实工作流优化。
- 保持 `UI_THEME_GUIDE.md` 中规定的亮色黑白极简风格。
- 优先改进高频路径：文本库、编辑页、SRT 预览/导出。

## 当前前端概况

技术栈：

- Vue 3
- Pinia
- Vue Router
- Ant Design Vue 4
- Axios
- Vite

主要页面：

- `web/src/views/TextList.vue`：文本库。
- `web/src/views/TextEdit.vue`：文本编辑。
- `web/src/views/Import.vue`：文本导入。

主要组件：

- `FolderTree.vue`：文件夹树。
- `TextTable.vue`：文本表格。
- `TagSelector.vue`：标签选择。
- `SrtPreviewModal.vue`：SRT 预览。
- `QuickGenerateModal.vue`：快速生成。
- `BatchImportModal.vue`：批量导入。
- `VoiceSynthModal.vue`：语音合成。
- `ApiSettingsModal.vue`：API 设置。

## 优先级总览

### P0：最值得优先做

- 统一 SRT 参数面板。
- 编辑页实时 SRT 预览。
- 自动保存与未保存提醒。
- 导出前确认预览流程。

### P1：显著提升效率

- 文本库批量操作。
- 首页工具栏减压。
- 字幕质量检查。
- 更明确的空状态和错误提示。

### P2：锦上添花

- 最近使用/收藏。
- 默认导出设置。
- 更完整的语音合成工作流状态。

## 功能 1：统一 SRT 参数面板

### 问题

当前多个地方硬编码字幕参数，例如：

- `speed: 5`
- `max_chars: 20`

这些参数分散在预览、导出、快速生成、双语导出等流程里。用户无法直观控制导出结果。

### 目标

新增统一参数组件，例如 `SrtOptionsPanel.vue`，在相关流程中复用。

建议参数：

- 语速：`speed`，单位字/秒。
- 每段最大字数：`max_chars`。
- 是否保留标点。
- 是否启用双语模式。
- 双语模式下的目标语言。
- 导出文件名后缀。

### 技术方案

新增组件：

- `web/src/components/SrtOptionsPanel.vue`

新增或扩展 store：

- 可在 `web/src/stores/settings.js` 中保存默认字幕参数。

使用位置：

- `TextTable.vue` 导出前弹窗。
- `SrtPreviewModal.vue` 预览参数。
- `QuickGenerateModal.vue` 快速生成参数。
- `TextEdit.vue` 编辑页实时预览参数。

### 验收标准

- 用户可以在导出前修改语速和每段最大字数。
- 参数在预览和导出中保持一致。
- 默认参数可保存到本地设置。
- 不再在多个组件中散落硬编码字幕参数。

## 功能 2：编辑页实时 SRT 预览

### 问题

当前用户编辑文本后，需要保存、返回列表、点击预览，才能看到分段效果。这个流程打断写稿节奏。

### 目标

在 `TextEdit.vue` 中增加实时预览区域，让用户边写边看字幕分段结果。

建议交互：

- 桌面端：正文编辑区和 SRT 预览区左右分栏。
- 窄屏：预览区折叠为下方区域或 Tab。
- 预览支持 debounce，避免每次输入都请求后端。
- 空内容时显示轻量提示。

### 技术方案

新增组件：

- `web/src/components/SrtLivePreview.vue`

实现方式：

- 输入内容变化后 debounce 500ms。
- 调用现有 SRT 生成接口，或前端复用本地分段逻辑的轻量预估。
- 加载中显示小型状态，不遮挡编辑器。
- 请求失败时显示具体错误。

### 验收标准

- 编辑文本时无需离开页面即可看到预计分段。
- 修改参数后预览同步更新。
- 长文本输入时界面不卡顿。
- 预览区域符合黑白极简主题。

## 功能 3：自动保存与未保存提醒

### 问题

编辑长文本时，用户可能误关页面、切换路由或刷新，导致未保存内容丢失。

### 目标

为编辑页增加草稿保护机制。

建议能力：

- 本地自动保存草稿。
- 显示保存状态：已保存 / 有未保存修改 / 自动保存于 xx:xx。
- 离开页面前提醒未保存修改。
- 新建文本时也能恢复未提交草稿。

### 技术方案

可新增 composable：

- `web/src/composables/useDraftAutosave.js`

存储方式：

- `localStorage`
- key 示例：`moying:draft:new`、`moying:draft:text:<id>`

路由拦截：

- 使用 Vue Router navigation guard。
- 浏览器刷新使用 `beforeunload`。

### 验收标准

- 输入内容后刷新页面，可以恢复草稿。
- 保存成功后清除对应草稿。
- 有未保存修改时离开页面会提醒。
- 状态文案清晰但不打扰。

## 功能 4：导出前确认预览流程

### 问题

当前导出偏“点击即下载”，用户无法在下载前确认参数和内容。

### 目标

统一导出体验为：

1. 点击导出。
2. 打开导出弹窗。
3. 用户调整参数。
4. 查看预览。
5. 确认下载。

### 技术方案

新增组件：

- `web/src/components/SrtExportModal.vue`

能力：

- 复用 `SrtOptionsPanel.vue`。
- 内置 SRT 预览。
- 支持中文字幕和中英双语。
- 下载按钮只在预览成功后可用。

### 验收标准

- 首页表格和编辑页都使用同一个导出弹窗。
- 用户能在导出前看到最终 SRT 内容。
- API Key 缺失、文本为空、翻译失败等错误有明确提示。

## 功能 5：字幕质量检查

### 问题

字幕生成后可能出现段落过长、空段、标点异常等问题，用户需要手动排查。

### 目标

新增轻量质量检查器，帮助用户在导出前发现明显问题。

检查项建议：

- 单段字数超过上限。
- 文本中存在连续空格或异常空行。
- 重复句子。
- 末尾缺少标点。
- 预计单段时长过短或过长。

### 技术方案

新增工具模块：

- `web/src/utils/subtitleQuality.js`

新增组件：

- `web/src/components/SubtitleQualityPanel.vue`

使用位置：

- `TextEdit.vue` 实时预览旁。
- `SrtExportModal.vue` 导出确认前。

### 验收标准

- 检查结果按严重程度显示。
- 点击问题可定位到对应段落或文本位置。
- 没有问题时显示简洁通过状态。

## 功能 6：文本库批量操作

### 问题

文本数量增多后，单个删除、单个移动文件夹、单个导出效率较低。

### 目标

为 `TextTable.vue` 增加多选和批量操作。

建议操作：

- 批量移动到文件夹。
- 批量添加标签。
- 批量删除。
- 批量导出 SRT。
- 批量生成双语字幕。

### 技术方案

改造组件：

- `TextTable.vue` 增加 `rowSelection`。
- `TextList.vue` 管理选中项和批量操作工具条。

可能新增组件：

- `BatchActionBar.vue`
- `MoveToFolderModal.vue`
- `BatchTagModal.vue`

后端可能需要补充：

- 批量更新文本文件夹接口。
- 批量打包导出接口。

### 验收标准

- 选中多篇文本后出现批量操作条。
- 批量删除需要二次确认。
- 批量操作完成后自动刷新列表。

## 功能 7：首页工具栏减压

### 问题

当前首页工具栏功能较多，视觉密度偏高：

- 设置
- 搜索
- 排序
- 语音合成
- 快速生成
- 批量导入
- 新建文本

### 目标

保留高频操作，把低频操作收纳到“更多”菜单。

建议布局：

- 左侧：页面标题和数量。
- 中间/右侧：搜索。
- 主要按钮：新建文本。
- 次要入口：导入。
- 低频入口：更多菜单，包含设置、语音合成、快速生成、批量导入。

### 技术方案

改造：

- `TextList.vue` header actions。

新增组件可选：

- `MoreActionsDropdown.vue`

### 验收标准

- 首页首屏更干净。
- 高频操作不超过 3 个显性按钮。
- 所有原功能仍可访问。

## 功能 8：空状态与错误提示优化

### 问题

空列表、空文件夹、接口失败等状态需要更清晰地告诉用户下一步该做什么。

### 目标

为空状态和错误状态增加具体引导。

建议：

- 没有文本：显示“新建第一篇文本”和“导入 txt”。
- 文件夹为空：提示可拖入文本或新建文本。
- 搜索无结果：提供清除搜索按钮。
- API Key 缺失：引导打开设置。
- 后端连接失败：提示检查服务是否运行。

### 技术方案

改造组件：

- `TextTable.vue`
- `FolderTree.vue`
- `QuickGenerateModal.vue`
- `VoiceSynthModal.vue`
- `ApiSettingsModal.vue`

可新增组件：

- `EmptyState.vue`
- `InlineError.vue`

### 验收标准

- 所有空状态都有下一步操作。
- 错误提示具体，不只显示“失败”。
- 提示文案保持中文、简洁、可行动。

## 功能 9：最近使用与收藏

### 问题

文本多了以后，文件夹和搜索仍然不足以快速回到常用内容。

### 目标

新增轻量组织能力：

- 收藏文本。
- 最近编辑。
- 最近导出。
- 未归档文本。

### 技术方案

前端优先方案：

- 收藏可先存 `localStorage`。
- 最近编辑/导出可由前端记录。

后端增强方案：

- `Text` 模型增加 `is_favorite`。
- 增加收藏切换接口。

### 验收标准

- 用户可以快速筛选收藏文本。
- 最近编辑列表能按时间显示。
- 不影响现有文件夹和标签逻辑。

## 推荐开发顺序

### 阶段 1：编辑和导出主流程

1. `SrtOptionsPanel.vue`
2. `SrtLivePreview.vue`
3. `SrtExportModal.vue`
4. 编辑页接入实时预览和导出弹窗

### 阶段 2：安全感和可控性

1. 自动保存草稿
2. 未保存离开提醒
3. 字幕质量检查
4. 具体错误提示

### 阶段 3：文本库效率

1. 首页工具栏减压
2. 表格多选
3. 批量移动/标签/删除
4. 批量导出

### 阶段 4：组织增强

1. 收藏文本
2. 最近编辑/最近导出
3. 未归档筛选

## 风格约束

开发以上功能时必须遵守：

- `UI_THEME_GUIDE.md`
- 不引入新的彩色主题。
- 不添加营销页式布局。
- 不使用复杂装饰背景。
- 不让按钮、表格、弹窗在窄屏溢出。
- 任何新增组件都应复用 `theme.css` 变量。

## 验证清单

每个阶段完成后至少执行：

```bash
cd web && pnpm run build
```

并手动检查：

- `http://localhost:3000/`
- `http://localhost:3000/edit`
- `http://localhost:3000/import`

涉及弹窗或导出时，还要检查：

- SRT 预览弹窗。
- 快速生成弹窗。
- 批量导入弹窗。
- 语音合成弹窗。
- API 设置弹窗。
