# UI Theme Guide

本项目 Web UI 采用「亮色黑白极简工作台」风格，参考 ChatGPT 式的低噪声工具界面。后续修改前端样式时，应优先遵守本文件，避免回到旧版金黑电影感或引入高饱和装饰。

## 核心原则

- 以内容和操作效率为中心，界面应安静、清晰、克制。
- 使用黑、白、灰建立层级，不使用彩色主题作为主要视觉语言。
- 通过留白、对齐、细边框、轻阴影体现质感，不依赖渐变、发光、纹理或装饰图形。
- 页面应像一个长期使用的文本工具，而不是营销页、海报页或视觉展示页。
- 功能逻辑和视觉样式分离，新增功能优先复用现有 Ant Design Vue 组件和全局 CSS 变量。

## 色彩规范

主要颜色来自 `web/src/styles/theme.css`，新增样式必须优先使用 CSS 变量。

- 页面背景：`--paper`，近白灰。
- 面板背景：`--surface` / `--paper-soft`。
- 悬停背景：`--surface-hover`。
- 选中背景：`--surface-active`。
- 边框：`--surface-border` / `--surface-border-strong`。
- 主文本：`--text-primary`。
- 次级文本：`--text-secondary`。
- 弱提示文本：`--text-muted` / `--text-subtle`。
- 主按钮：黑底白字，使用 `--text-primary` 和 `--text-inverse`。

禁止事项：

- 不要新增金色、红色、紫色、蓝色渐变等主题色。
- 不要使用发光、霓虹、厚重投影、径向渐变、纹理噪点做常规装饰。
- 不要把 `--gold`、`--vermillion` 当作真实彩色使用。它们只是为了兼容旧样式而保留的黑白别名。

## 字体与文字

- 字体使用系统无衬线栈：`--font-body` 和 `--font-display`。
- 不使用夸张字距。常规 `letter-spacing` 应保持 `0`。
- 中文标题保持清晰即可，不需要书法、复古、电影感字体。
- 页面标题建议 `24px - 28px`，字重 `600 - 650`。
- 表格、标签、说明文本保持紧凑，避免过大字号。

## 布局与容器

- 全局页面背景保持 `--paper`。
- 主内容区域使用白色面板、浅灰边框和轻微阴影。
- 面板圆角使用 `--radius-md` 或 `--radius-lg`，不要使用大圆角卡片堆叠。
- 表格、编辑器、上传区必须有稳定边界，避免视觉漂浮。
- 移动端和窄窗口下要优先保证内容不溢出，必要时隐藏次要列或换行。

## 组件状态

- 默认按钮：白底、浅灰边框、黑/深灰文字。
- 主按钮：黑底白字。
- 悬停态：轻微改变边框或背景即可，不做位移和发光。
- 聚焦态：使用 `--shadow-focus`。
- 选中态：使用 `--surface-active`，必要时加黑色细边或左侧 3px 黑色标识。
- 危险操作可以使用低饱和红色提示，但只能用于删除等风险动作，不能作为主题色。

## 页面约定

### 顶部导航

- 背景使用半透明近白灰，可保留轻微模糊。
- 品牌字使用黑色，字距为 0。
- 选中菜单使用浅灰背景，不使用彩色下划线或发光。

### 文本库

- 左侧文件夹和右侧表格保持工具台布局。
- 表格表头使用浅灰底，正文白底。
- 行 hover 使用 `--surface-hover`。
- 标签使用灰底/细边框的胶囊样式。

### 编辑页

- 编辑区优先保持文档感，标题输入和正文输入应干净、低干扰。
- 正文区域使用浅色背景和细边框。
- 字数、预计字幕段数等辅助信息使用弱文本颜色。

### 导入页

- 上传区域使用浅色背景和虚线边框。
- 图标保持黑白灰，不使用彩色强调。
- 文件选中状态可以将边框改为黑色。

### 弹窗

- 弹窗背景使用白色，边框浅灰，阴影轻。
- 弹窗内预览代码块/SRT 内容使用 `--paper-soft`。
- 语音合成、快速生成、API 设置等弹窗不得引入独立彩色主题。

## 修改检查清单

每次修改 UI 前后，至少检查：

- 是否使用了 `theme.css` 中的变量，而不是硬编码彩色值。
- 是否新增了高饱和色、渐变、发光或纹理。
- 是否存在文字溢出、按钮挤压、表格横向截断。
- 首页、编辑页、导入页的视觉是否仍然统一。
- 弹窗、下拉菜单、标签、表格 hover/selected/focus 状态是否仍是黑白灰。
- 执行 `cd web && pnpm run build`，确认构建通过。

## 推荐验证方式

开发完成后启动服务：

```bash
./start.sh start
```

在浏览器检查：

- `http://localhost:3000/`
- `http://localhost:3000/edit`
- `http://localhost:3000/import`

如果修改影响组件或弹窗，还应打开对应功能入口进行视觉确认。
